Shared day navigator (Today + Map): chevrons step ±1 day, center shows the date plus a TODAY marker or JUMP TO TODAY action.

```jsx
<DayPicker date={selectedDate} onChange={setSelectedDate} />
```

- Chevrons are 44pt circular targets; JUMP TO TODAY appears whenever you're off today.
